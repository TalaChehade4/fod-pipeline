# Environment / secrets
.env

# Python
__pycache__/
*.pyc
*.egg-info/
build/
dist/
.pytest_cache/
.coverage
htmlcov/

# Virtual envs
.venv/
venv/

# Data & model artifacts 
# Anchored to repo root so it doesn't also match src/fod_pipeline/data/,
# the bundled MobileCLIP prompt resource that ships with the package.
/data/
*.manifest
*.pt
*.pth
*.tar.gz
join-config*.json
*_old.csv
label_map*.json
object_labels*.json
embeddings*.json

# Outputs (anchored to repo root - see /data/ note above; these are the
# default --output-dir names the CLIs write to when run locally)
/outputs/
/PreparedData/
/FinalModel/
*.png
*.csv
!tests/**/*.csv

# Mobile export artifacts (fod-export-*) - regenerated locally from the
# weights above, never committed.
/mobile_models/
*.mlpackage/
*.onnx

# OS/editor
.DS_Store
.vscode/
.idea/
